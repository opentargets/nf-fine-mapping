{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/opentargets/nf-fine-mapping/main/nextflow_schema.json",
  "title": "Open Targets multi-ancestry fine-mapping pipeline parameters",
  "description": "Parameters for the Open Targets multi-ancestry fine-mapping Nextflow pipeline.",
  "type": "object",
  "additionalProperties": false,
  "required": ["manifest", "output_dir", "route"],
  "properties": {
    "manifest": {"type": "string", "format": "file-path", "description": "Tab-separated manifest describing studies and summary-statistics inputs.", "pattern": "^\\S+\\.(tsv|txt)$", "fa_icon": "fas fa-file-lines"},
    "manifest_base_dir": {"type": "string", "format": "directory-path", "default": ".", "description": "Base directory used to resolve relative manifest paths.", "fa_icon": "fas fa-folder-open"},
    "output_dir": {"type": "string", "format": "directory-path", "description": "Directory where pipeline outputs and reports are written.", "fa_icon": "fas fa-folder-open"},
    "route": {"type": "string", "description": "Manifest route to execute.", "fa_icon": "fas fa-route"},
    "locus_breaker_method": {"type": "string", "enum": ["collector", "gentropy"], "default": "collector", "description": "Implementation used for ancestry-specific locus breaking. Collector is the supported default; Gentropy is an opt-in, slower alternative."},
    "canonical_region_min_maf": {"type": "number", "minimum": 0, "exclusiveMaximum": 0.5, "default": 0.01, "description": "Strict minimum MAF for canonical-region variants, calculated as min(EAF, 1-EAF)."},
    "locus_breaker_large_loci_size": {"type": "integer", "minimum": 1, "default": 1500000, "description": "Locus size threshold above which the collector locus-breaker replaces a locus with fixed-width WBC leads; every emitted locus is bounded to this size."},
    "canonical_region_max_region_span_bp": {"type": "integer", "minimum": 1, "default": 3000000, "description": "Inclusive maximum span of a merged canonical region in base pairs."},
    "ld_annotation_method": {"type": "string", "enum": ["gentropy", "hailing_ducks"], "default": "hailing_ducks", "description": "Backend used for ancestry-specific LD annotation. Hailing Ducks is the supported default; Gentropy is an opt-in, slower alternative."},
    "ld_registry": {
      "type": "array", "default": [], "description": "Ancestry-specific LD reference registry.",
      "items": {"type": "object", "required": ["ancestry", "bm_path"], "additionalProperties": false, "properties": {"ancestry": {"type": "string", "minLength": 1}, "vi_path": {"type": "string", "format": "directory-path"}, "ht_path": {"type": "string"}, "bm_path": {"type": "string"}}},
      "fa_icon": "fas fa-database"
    },
    "hailing_ducks_container": {"type": "string", "default": "ghcr.io/project-defiant/hailing-ducks:v1.1.0", "description": "Container image used by Hailing Ducks LD annotation."},
    "hailing_ducks_max_cached_blocks": {"type": "integer", "minimum": 1, "default": 8, "description": "Maximum Hailing Ducks LD blocks cached per task."},
    "fine_mapping_methods": {"type": "array", "default": ["multisusie"], "description": "Fine-mapping methods to execute.", "items": {"type": "string", "enum": ["multisusie", "susiex", "sushie"]}},
    "multisusie_container": {"type": "string", "default": "ghcr.io/project-defiant/multisusie:sha256-db07e794d00c8cac8bfc162fa43015254612a2ff627641078e09d0695339c8a5", "description": "Container image used by MultiSuSiE."},
    "multisusie_purity_min_r2": {"type": "number", "exclusiveMinimum": 0, "exclusiveMaximum": 1, "default": 0.01, "description": "Minimum credible-set purity on the R-squared scale for reportable MultiSuSiE components."},
    "susiex_container": {"type": "string", "default": "ghcr.io/project-defiant/susiex/susiex:sha256-bf751e492d4bb3b7267f58a0583a1e00c3a9fc8dca3c1e565758d9e4754698cc", "description": "Container image used by SuSiEx."},
    "sushie_container": {"type": "string", "default": "ghcr.io/project-defiant/sushie/sushie:sha256-adedefcfe352f0ea80345654c6969e78a351ac333b10cd054721e32dd9b182d6", "description": "Container image used by SuShiE."},
    "collector_container": {"type": "string", "description": "Collector image used by cloud profiles."},
    "gentropy_spark_uri": {"type": "string", "default": "local[*]", "description": "Spark URI passed to Gentropy."},
    "gentropy_spark_conf": {"type": "string", "default": "{}", "description": "JSON Spark configuration passed to Gentropy."},
    "gentropy_ld_annotation_spark_conf": {"type": "string", "description": "Optional Spark configuration for Gentropy LD annotation."},
    "empty_status_stub_emit": {"type": "boolean", "default": false, "hidden": true, "description": "Emit empty-status records during stub execution."},
    "ld_pair_stats_stub_emit": {"type": "boolean", "default": false, "hidden": true, "description": "Emit LD-pair status records during stub execution."},
    "ld_pair_stats_stub_empty_locus_set_ids": {"type": "array", "default": [], "hidden": true, "description": "Locus-set IDs receiving empty-LD status in stub tests.", "items": {"type": "string"}},
    "trace_report_suffix": {"type": "string", "description": "Suffix for trace, report, timeline, and DAG filenames."},
    "validate_params": {"type": "boolean", "default": true, "hidden": true, "description": "Validate pipeline parameters against this schema."}
  }
}
